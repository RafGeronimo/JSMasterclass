// A operação size disponível para o Map retorna a quantidade de elementos que existem no mapa

const timeUnits = new Map([["second", 1], ["minute", 60], ["hour" , 3600]]);
console.log(timeUnits);
console.log(timeUnits.size)