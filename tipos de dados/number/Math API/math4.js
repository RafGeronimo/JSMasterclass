console.log(Math.min(1,2,3,4,5,6)); // Pega o menor valor entre os parâmetros
console.log(Math.max(1,2,3,4,5,6)); // pega o maior valor entre os parâmetros
console.log(Math.random()); // retorna um número aleatório entre 0 e 1.

//se queremos que o numero seja na ordem de 1000 e inteiro:

console.log(Math.floor(Math.random()*1000));
